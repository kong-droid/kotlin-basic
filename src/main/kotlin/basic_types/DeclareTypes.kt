package basic_types
/** memo **/
// 초기화 하지 않고 변수를 선언하려면 :로 변수 유형을 지정한다.
fun main() {
    // 변수값 선언없이 초기화한다.
    val d: Int;

    // 변수 선언
    d = 3

    // 변수값 선언, 타입 지정
    val e: String = "hello";


    // 초기화 된 정보를 읽어준다.
    println(d);
    println(e);
}